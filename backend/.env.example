# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Use DATABASE_URL for production (e.g., from Render, Heroku, etc.)
# Or use POSTGRESQL_URI for custom PostgreSQL connection
DATABASE_URL=postgresql://username:password@localhost:5432/edumaster
# Alternative: POSTGRESQL_URI=postgresql://username:password@localhost:5432/edumaster

# JWT Secret Key
# Generate a strong random secret for production
# You can use: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:3002
